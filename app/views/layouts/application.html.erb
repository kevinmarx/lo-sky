<!DOCTYPE html>
<html
  style="background: -moz-radial-gradient(center, ellipse bottom,  <%= @resource.first.colors.first %> 0%, <%= @resource.first.colors.second %> 20%, <%= @resource.first.colors.third %> 42%, <%= @resource.first.colors.last %> 100%);
  background: -webkit-radial-gradient(center, ellipse bottom,  <%= @resource.first.colors.first %> 0%, <%= @resource.first.colors.second %> 20%,<%= @resource.first.colors.third %> 42%,<%= @resource.first.colors.last %> 100%);
  background: radial-gradient(ellipse at bottom,  <%= @resource.first.colors.first %> 0%, <%= @resource.first.colors.second %> 20%,<%= @resource.first.colors.third %> 42%,#<%= @resource.first.colors.last %>100%);"
>
<head>
  <title>Lo-Sky</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<%= yield %>

</body>
</html>
